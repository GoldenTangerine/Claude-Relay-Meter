# 源代码文件（已编译到 out/）
src/**
tsconfig.json

# 开发和测试文件
.vscode/**
.vscode-test/**
.claude/**

# 构建和依赖
# 注意：不排除 node_modules，让 vsce 自动包含所有生产依赖
# 这样可以确保所有嵌套依赖都被正确打包

# Git 相关
.git/**
.gitignore

# 文档（保留必要的）
FINAL_CHECK_REPORT.txt
SECURITY_CHECK.md
plan.md

# 编译和打包
builds/**
*.vsix

# 日志和临时文件
*.log
npm-debug.log*
.DS_Store

# 配置示例
.vscode/settings.json.example

# ESLint 配置
.eslintrc*
eslint.config.mjs
